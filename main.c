#include <stdio.h>
#include <windows.h>

int main(){

SetConsoleCP(65001);
SetConsoleOutputCP(65001);

for(int i = 0; i < 10000; i++){
    printf("%s %d", "\nOs numeros estao aumentando: ---->", i);
}

    printf("\nmeu for funfo");

return 0;

}